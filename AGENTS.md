# 仓库开发规则

本文件是规则入口，只做引用与登记；具体规则全部放在 `rules/` 目录。处理、审查或重构任何代码前，必须（MUST）先读取并遵循命中的规则文件。

## 规则索引

| 文件 | 适用范围 | 触发条件 |
| --- | --- | --- |
| `rules/clean-code.md` | 全语言通用 | 任何代码、测试、重构、审查或文档变更（默认加载） |
| `rules/python.md` | Python | 写、改、审查或重构任何 `.py` 文件或 Python 工程配置前 |
| `rules/frontend.md` | 前端 | 写、改、审查或重构任何前端资源前（`.html`/`.css`/`.less`/`.scss`/`.js`/`.jsx`/`.ts`/`.tsx`/`.vue`/`.svelte`，及页面、组件、路由、样式、前端状态管理与构建配置、ESLint/Prettier 配置） |
| `rules/ponytail.md` | 全语言通用 | 实现/编码类任务的风格兜底：以「懒而高效」为先——最少代码、最少抽象、复用现有模式 |

多个规则同时适用时（例如前端的 TypeScript 源码与构建配置），一并加载并遵守。

## 优先级

规则冲突时，从高到低：**用户明确指示 > `rules/<语言或领域>.md` 专属规则 > `rules/clean-code.md` 通用规则**。专属规则是对通用规则的细化，冲突时以更具体者为准。

## 登记约定

新增 `rules/<语言或领域>.md` 时，必须同步在上面的「规则索引」表登记其路径、适用范围与触发条件，否则规则永不生效。
