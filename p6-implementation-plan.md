# P6 实施方案：区域分流 + 企查查接入

> 2026-09-20。P6 定义（`company-discovery-plan.md` §八）：中国大陆场景接企查查；跨境场景接海关数据。依赖 P5（补齐层，已建成）。

## 一、P6 与 P4/P5 的同构与差异

**同构**：还是「manifest + is_configured() + 契约方法 + 凭据就绪才注册」的 adapter 模式，
测试继续用假 client（零网络），缓存/合并/缺失上报全部复用。

**三处新东西**：

| 新东西 | 说明 |
| --- | --- |
| **没有官方 Python SDK** | P4/P5 用的 tavily-python / peopledatalabs 都是现成库；企查查开放平台只有 REST，需要自己写签名客户端（`httpx` + MD5 签名） |
| **区域分流** | 一次挖掘的公司可能横跨中国大陆与海外，需要按域名/国别把 `EnrichTarget` 分给不同源——建议用 `SourceManifest.regions`（"CN" / "GLOBAL"）表达，`collect()` 编排时按区域过滤候选源 |
| **按次计费是真的贵** | 工商信息约 1 元/次、工商详情约 2 元/次。缓存命中、区域过滤、只查缺字段的公司，从「锦上添花」变成「成本控制必需」 |

## 二、企查查开放平台现状（2026-09 调研）

- 入口：`openapi.qcc.com`，注册 → **企业实名认证** → **应用场景审核** → 开通接口。
- ⚠️ 个人开发者卡点：企业认证 + 场景审核，与 PDL 的 work email 是同类门槛，需要企业主体。
- 价格参考：企业工商信息（ApiCode 410）约 1 元/次；企业工商详情（735）约 2 元/次，
  套餐 1000 元/500 次；企业模糊搜索（886）约 0.2 元/次。部分接口有 **免费试用 20 次**。
- 鉴权：`Token + SECKEY`，`Token = MD5(SECKEY + TIMESPAN)`，每次请求带 timespan。

## 三、实施步骤

### 6a. 架构准备：区域分流（半天）
- `contracts.py`：`SourceManifest.regions` 已存在，给 `CompanyEnrichQuery.collect` 编排加
  「按 target 国别选源」的过滤（域名 `.cn` / location 含中国 → CN 源，否则 GLOBAL 源）。
- `EnrichTarget` 加 `region` 派生属性（`_is_cn()`：`.cn` 域名或 location 命中中国地名表）。
- 这一步不花钱，纯重构，现有测试继续过。

### 6b. QccSource 骨架（1 天）
- `providers/qcc_source.py`：manifest（`company_enrich` capability，regions=("CN",)，
  `cost_per_call=1.0`），`is_configured()` 检查 `config.QCC_SECKEY`。
- `config.py` 加 `QCC_TOKEN / QCC_SECKEY`（env `AIGET_QCC_*`）。
- `_sign()` 签名函数单独导出，方便单测（MD5 结果确定性，不用 mock）。
- 映射：统一社会信用代码 → `external_id`；注册资本/成立日期/登记状态 → `attributes`；
  经营范围关键词 → `industries`；法定代表人 → `attributes["legal_rep"]`。
  **只存照面信息，不爬、不二次分发**（《个保法》《数据安全法》边界）。

### 6c. 测试 + 试用验证（半天）
- 假 client 单测：映射、缓存、错误分类（企查查错误码 `Status/Message` 结构）、签名函数。
- 用免费试用的 20 次做一次真实链路冒烟（挑 3~5 家确定性高的中国公司）。

### 6d. 成本护栏（半天）
- 一次挖掘 25 家 = 约 25 元；月挖 40 次 ≈ 1000 元/月，这就是为什么要：
  1. 只对 **CN 区域且缺字段** 的公司调 QCC（`missing_fields` 驱动，已有）；
  2. TTL 缓存命中即跳过（已有，P7 换 SQLite 后跨进程生效）；
  3. 前端保留「字段缺失 → 重试」入口，而不是自动重试烧钱。

## 四、海关数据（跨境）：建议暂缓

- 公司级海关交易数据（Volza / ImportGenius）年费数千美金起，且对当前产品形态增益有限
  （B2B 销售线索主要靠工商信息 + 联系人，不是提单记录）。
- 建议只在 `CompanyRecord.attributes` 里预留 `customs_records` 键位，等有真实跨境需求再采购。

## 五、如果企业认证短期办不下来

P6 可以整体后置，**先做 P7（SQLite 持久化）**：成本几乎为零、对真实源的配额保护立竿见影，
且进程内 TTL 缓存重启即失——P7 完成后每次挖掘才真正「复用」Tavily 的结果。
P6 只需要企业主体材料到位后，按 6b/6c 各花一天就能插进瀑布。

## 六、免费替代路径（2026-09-20 补充调研）

**结论先行：中国大陆「权威工商照面数据」没有合规的免费 API。** 免费路只有三条，价值从高到低：

### 6-免1. 用已有 Tavily 做轻量富化（✅ 已实现，2026-09-20 晚）
- 实现：`TavilySource` 扩展 `company_enrich` 能力（manifest capabilities 两项，priority=40
  排在权威源之后），新增 `enrich_companies()`——只对 `EnrichTarget.missing` 含 `summary`
  的目标做一次针对性检索（`{名称} 企业简介`，5 条结果），通过相关性校验
  （名称出现在标题/正文，或域名主体出现在 URL）后取第一条相关摘要补 `summary` + 证据。
- 编排层 `EnrichTarget` 新增 `missing` 提示字段（`enrich_companies` 从召回记录的
  `missing_fields` + 空 summary 派生）——权威源可忽略，按次计费源靠它省 credit。
- 成本护栏：缺摘要才检索 + 进程内缓存（含「确认无相关页面」空哨兵）+
  逐目标容错（单家失败不拖垮整批，全部失败才抛 SourceError）。
- 诚实边界：只补叙述性摘要，照面字段不抽；`attributes["source"]="web_search"` 如实标注。
- 验证：22 个单测全过；真实链路冒烟通过（云杉网络 → 36氪项目页摘要 + 证据 URL）。

### 6-免2. 政府公共数据开放平台（真实免费 API，但覆盖碎）
- 各省数据开放网有**无条件开放**的工商注册信息 API（如山东 `data.sd.gov.cn`，HmacSHA256 签名，
  返回企业名称/成立日期/注册资金/经营范围/统一社会信用代码等字段）。
- 限制：通常**限本省**、更新滞后、各地字段格式不一。适合做特定省份场景，不适合全国通用。

### 6-免3. 场景限定的免费权威源（窄但合规）
- **上市公司**：巨潮资讯（cninfo，深交所官方，公开接口查公司资料/公告）；美股走 SEC EDGAR（完全免费 API）。
- **gsxt.gov.cn（国家企业信用信息公示系统）**：权威免费，但无公开 API、强验证码，只可人工单查，
  **批量程序化访问有合规边界**（这正是企查查等商业 API 存在的理由）。
- **OpenCorporates**：海外注册数据有免费层，不覆盖中国大陆。

### 建议
免费路径里 **6-免1 性价比最高**（一次 Tavily 富化查询 ≈ 0.01 credit，今天就能实现）；
6-免2/3 按需接入。权威照面字段（注册资本、股东、法人）想规模化拿到，最终还是绕回
企查查企业认证——免费路与付费路的分界线就在「照面字段的权威性」上。

### 6-免1b. 百度搜索 API（推荐，比 Tavily 更适合中国场景，2026-09-20 调研）

**为什么百度是正解**：爱企查（aiqicha.baidu.com）是百度自家产品，百度搜索对它收录极好；
爱企查页面含**照面信息**（注册资本、法定代表人、成立日期、登记状态），摘要格式相对规整——
用「指定站点搜索」精准命中后，照面字段有机会比泛搜索**可靠得多**地抽出来。

**产品与价格**（百度智能云千帆「百度搜索」，2026-09 现价）：
- 标准版 **0.036 元/次**，增强版 0.072 元/次；**每月免费 1500 次**（按天发约 50 次/天）
- 一次挖掘 25 家缺摘要公司：每天前 50 次全免费 → **日常挖掘成本为 0**
- 返回 URL/标题/摘要/发布时间；支持**指定站点搜索**（`site:aiqicha.baidu.com`）与时间筛选
- 接口：OpenAI 兼容 chat/completions 风格（`/v2/ai_search/...`），API Key（Bearer）鉴权，
  query 单轮、**限 72 字符**；另有 MCP 使用方式；默认 3 QPS
- 前置条件：百度智能云账号 + **个人实名认证即可**（不需要企业认证——比企查查门槛低得多）

**实施方式**（半天）：新增 `BaiduSearchSource`，实现 `company_enrich`，**priority=30**
（PDL 20 之后、Tavily web 富化 40 之前）。查询策略两级：
1. `site:aiqicha.baidu.com {公司名}` → 命中爱企查页则抽照面字段（摘要里含「注册资本/法定代表人/成立日期」时才写，`attributes["source"]="aiqicha_web"`）
2. 未命中再退化到泛查询（同 Tavily 富化逻辑，只补摘要）

**不要走的路**：抓百度搜索结果页（SERP 爬虫）——反爬强、违反 ToS、有合规风险。必须走官方 API。

**与 Tavily 富化的关系**：并存而非替换——百度管中国公司（爱企查站点限定），Tavily 管全球兜底；
瀑布优先级 PDL(20) > 百度(30) > Tavily(40) 天然表达这个分工。
