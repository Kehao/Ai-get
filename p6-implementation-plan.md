# P6 实施方案：区域分流 + 企查查接入

> 2026-09-20。P6 定义（`company-discovery-plan.md` §八）：中国大陆场景接企查查；跨境场景接海关数据。依赖 P5（补齐层，已建成）。

## 一、P6 与 P4/P5 的同构与差异

**同构**：还是「manifest + is_configured() + 契约方法 + 凭据就绪才注册」的 adapter 模式，
测试继续用假 client（零网络），缓存/合并/缺失上报全部复用。

**三处新东西**：

| 新东西 | 说明 |
| --- | --- |
| **没有官方 Python SDK** | P4/P5 用的 tavily-python / peopledatalabs 都是现成库；企查查开放平台只有 REST，需要自己写签名客户端（`httpx` + MD5 签名） |
| **区域分流** | 一次挖掘的公司可能横跨中国大陆与海外，需要按域名/国别把 `EnrichTarget` 分给不同源——建议用 `SourceManifest.regions`（"CN" / "GLOBAL"）表达，`collect()` 编排时按区域过滤候选源 |
| **按次计费是真的贵** | 工商信息约 1 元/次、工商详情约 2 元/次。缓存命中、区域过滤、只查缺字段的公司，从「锦上添花」变成「成本控制必需」 |

## 二、企查查开放平台现状（2026-09 调研）

- 入口：`openapi.qcc.com`，注册 → **企业实名认证** → **应用场景审核** → 开通接口。
- ⚠️ 个人开发者卡点：企业认证 + 场景审核，与 PDL 的 work email 是同类门槛，需要企业主体。
- 价格参考：企业工商信息（ApiCode 410）约 1 元/次；企业工商详情（735）约 2 元/次，
  套餐 1000 元/500 次；企业模糊搜索（886）约 0.2 元/次。部分接口有 **免费试用 20 次**。
- 鉴权：`Token + SECKEY`，`Token = MD5(SECKEY + TIMESPAN)`，每次请求带 timespan。

## 三、实施步骤

### 6a. 架构准备：区域分流（半天）
- `contracts.py`：`SourceManifest.regions` 已存在，给 `CompanyEnrichQuery.collect` 编排加
  「按 target 国别选源」的过滤（域名 `.cn` / location 含中国 → CN 源，否则 GLOBAL 源）。
- `EnrichTarget` 加 `region` 派生属性（`_is_cn()`：`.cn` 域名或 location 命中中国地名表）。
- 这一步不花钱，纯重构，现有测试继续过。

### 6b. QccSource 骨架（1 天）
- `providers/qcc_source.py`：manifest（`company_enrich` capability，regions=("CN",)，
  `cost_per_call=1.0`），`is_configured()` 检查 `config.QCC_SECKEY`。
- `config.py` 加 `QCC_TOKEN / QCC_SECKEY`（env `AIGET_QCC_*`）。
- `_sign()` 签名函数单独导出，方便单测（MD5 结果确定性，不用 mock）。
- 映射：统一社会信用代码 → `external_id`；注册资本/成立日期/登记状态 → `attributes`；
  经营范围关键词 → `industries`；法定代表人 → `attributes["legal_rep"]`。
  **只存照面信息，不爬、不二次分发**（《个保法》《数据安全法》边界）。

### 6c. 测试 + 试用验证（半天）
- 假 client 单测：映射、缓存、错误分类（企查查错误码 `Status/Message` 结构）、签名函数。
- 用免费试用的 20 次做一次真实链路冒烟（挑 3~5 家确定性高的中国公司）。

### 6d. 成本护栏（半天）
- 一次挖掘 25 家 = 约 25 元；月挖 40 次 ≈ 1000 元/月，这就是为什么要：
  1. 只对 **CN 区域且缺字段** 的公司调 QCC（`missing_fields` 驱动，已有）；
  2. TTL 缓存命中即跳过（已有，P7 换 SQLite 后跨进程生效）；
  3. 前端保留「字段缺失 → 重试」入口，而不是自动重试烧钱。

## 四、海关数据（跨境）：建议暂缓

- 公司级海关交易数据（Volza / ImportGenius）年费数千美金起，且对当前产品形态增益有限
  （B2B 销售线索主要靠工商信息 + 联系人，不是提单记录）。
- 建议只在 `CompanyRecord.attributes` 里预留 `customs_records` 键位，等有真实跨境需求再采购。

## 五、如果企业认证短期办不下来

P6 可以整体后置，**先做 P7（SQLite 持久化）**：成本几乎为零、对真实源的配额保护立竿见影，
且进程内 TTL 缓存重启即失——P7 完成后每次挖掘才真正「复用」Tavily 的结果。
P6 只需要企业主体材料到位后，按 6b/6c 各花一天就能插进瀑布。

## 六、免费替代路径（2026-09-20 补充调研）

**结论先行：中国大陆「权威工商照面数据」没有合规的免费 API。** 免费路只有三条，价值从高到低：

### 6-免1. 用已有 Tavily 做轻量富化（✅ 已实现，2026-09-20 晚）
- 实现：`TavilySource` 扩展 `company_enrich` 能力（manifest capabilities 两项，priority=40
  排在权威源之后），新增 `enrich_companies()`——只对 `EnrichTarget.missing` 含 `summary`
  的目标做一次针对性检索（`{名称} 企业简介`，5 条结果），通过相关性校验
  （名称出现在标题/正文，或域名主体出现在 URL）后取第一条相关摘要补 `summary` + 证据。
- 编排层 `EnrichTarget` 新增 `missing` 提示字段（`enrich_companies` 从召回记录的
  `missing_fields` + 空 summary 派生）——权威源可忽略，按次计费源靠它省 credit。
- 成本护栏：缺摘要才检索 + 进程内缓存（含「确认无相关页面」空哨兵）+
  逐目标容错（单家失败不拖垮整批，全部失败才抛 SourceError）。
- 诚实边界：只补叙述性摘要，照面字段不抽；`attributes["source"]="web_search"` 如实标注。
- 验证：22 个单测全过；真实链路冒烟通过（云杉网络 → 36氪项目页摘要 + 证据 URL）。

### 6-免2. 政府公共数据开放平台（真实免费 API，但覆盖碎）
- 各省数据开放网有**无条件开放**的工商注册信息 API（如山东 `data.sd.gov.cn`，HmacSHA256 签名，
  返回企业名称/成立日期/注册资金/经营范围/统一社会信用代码等字段）。
- 限制：通常**限本省**、更新滞后、各地字段格式不一。适合做特定省份场景，不适合全国通用。

### 6-免3. 场景限定的免费权威源（窄但合规）
- **上市公司**：巨潮资讯（cninfo，深交所官方，公开接口查公司资料/公告）；美股走 SEC EDGAR（完全免费 API）。
- **gsxt.gov.cn（国家企业信用信息公示系统）**：权威免费，但无公开 API、强验证码，只可人工单查，
  **批量程序化访问有合规边界**（这正是企查查等商业 API 存在的理由）。
- **OpenCorporates**：海外注册数据有免费层，不覆盖中国大陆。

### 建议
免费路径里 **6-免1 性价比最高**（一次 Tavily 富化查询 ≈ 0.01 credit，今天就能实现）；
6-免2/3 按需接入。权威照面字段（注册资本、股东、法人）想规模化拿到，最终还是绕回
企查查企业认证——免费路与付费路的分界线就在「照面字段的权威性」上。

### 6-免1b. 百度搜索 API（推荐，比 Tavily 更适合中国场景，2026-09-20 调研）

**为什么百度是正解**：爱企查（aiqicha.baidu.com）是百度自家产品，百度搜索对它收录极好；
爱企查页面含**照面信息**（注册资本、法定代表人、成立日期、登记状态），摘要格式相对规整——
用「指定站点搜索」精准命中后，照面字段有机会比泛搜索**可靠得多**地抽出来。

**产品与价格**（百度智能云千帆「百度搜索」，2026-09 现价）：
- 标准版 **0.036 元/次**，增强版 0.072 元/次；**每月免费 1500 次**（按天发约 50 次/天）
- 一次挖掘 25 家缺摘要公司：每天前 50 次全免费 → **日常挖掘成本为 0**
- 返回 URL/标题/摘要/发布时间；支持**指定站点搜索**（`site:aiqicha.baidu.com`）与时间筛选
- 接口：OpenAI 兼容 chat/completions 风格（`/v2/ai_search/...`），API Key（Bearer）鉴权，
  query 单轮、**限 72 字符**；另有 MCP 使用方式；默认 3 QPS
- 前置条件：百度智能云账号 + **个人实名认证即可**（不需要企业认证——比企查查门槛低得多）

**实施方式**（半天）：新增 `BaiduSearchSource`，实现 `company_enrich`，**priority=30**
（PDL 20 之后、Tavily web 富化 40 之前）。查询策略两级：
1. `site:aiqicha.baidu.com {公司名}` → 命中爱企查页则抽照面字段（摘要里含「注册资本/法定代表人/成立日期」时才写，`attributes["source"]="aiqicha_web"`）
2. 未命中再退化到泛查询（同 Tavily 富化逻辑，只补摘要）

**不要走的路**：抓百度搜索结果页（SERP 爬虫）——反爬强、违反 ToS、有合规风险。必须走官方 API。

**与 Tavily 富化的关系**：并存而非替换——百度管中国公司（爱企查站点限定），Tavily 管全球兜底；
瀑布优先级 PDL(20) > 百度(30) > Tavily(40) 天然表达这个分工。

### 6-免1c. 百度接管召回，Tavily 暂时关闭（2026-09-21 实施）

**开关**（都不必删密钥，随时改回）：

| 环境变量 | 值 | 作用 |
| --- | --- | --- |
| `AIGET_TAVILY_ENABLED` | `false` | Tavily 不注册（key 保留） |
| `AIGET_COMPANY_SOURCE` | `baidu` | 公司召回走百度；留空则跟随 `AIGET_DATA_SOURCE` |

**为什么公司源与默认源要分成两个键**：百度只有公司召回能力，没有人物档案能力。
把 `AIGET_DATA_SOURCE` 整体切到 `baidu` 会让「找人」在取值时直接报错，
所以新增 `AIGET_COMPANY_SOURCE` 单独管公司侧，「公司走真实检索、人物走演示语料」各行其是。

**百度源的能力扩展**：`capabilities=("company_search", "company_enrich")`。召回是两阶段检索：

1. **泛搜索**（不限站点）→ 目标是拿**企业官网域名**（域名才能去重与触达）；
2. 数量不足时→ **爱企查站点限定**补量（企业全名 + 照面字段，但没有企业域名，按名称去重）。

**共享映射模块 `web_mapping.py`**：两个网页检索源的域名提取、站点三分类、取名闸门都在这里，
避免两份实现慢慢漂移。其中取名闸门是被实测逼出来的——百度返回的标题里真的出现过
「News」「New」「Idc4」「智能云安全整体解决方案与服务提供商」，所以只有三类标题被采信
（组织形式词收尾 / 短且含行业词 / 与域名主段互证），其余退回域名主体。

**实测（2026-09-21，「找企业云安全服务商」，目标 25 家）**：

- 覆盖 **10~12 家（40~48%）**，成本 2 次调用（约 0.072 元，免费额度内为 0）；
- 爱企查段质量最高（「中汇云安全科技(上海)有限公司」+ 注册资本/法定代表人/成立日期），
  但**不稳定**：同一查询两次分别返回企业全名与「张霞」「Anlink安链云」这类人名/品牌页；
- 泛搜索段返回资讯与榜单类页面偏多（「2026一站式等保解决方案公司怎么选?」），
  标题大量是标语，取名闸门挡掉后退化到域名主体（Qingteng / Datacloudsec / Idc4）；
- 已修缺陷：**子域重复**——`saas.360.cn` 与 `ent.online.360.cn` 曾算两家，
  现在按注册域去重（`registrable_domain`）。

**公司侧也已改为标准派生（2026-09-21 修复）**：`_recall` 的两个分支原本实现不一致——人物侧
（后做的）用 `person_hints(criteria)` 从标准派生，公司侧却一直是最早的 `detect_city(query)` +
`industry_hints(query)`（**重新按关键词字典解析原文**），与 docstring 写的「召回提示从已冻结的
标准里派生」不符。现在新增 `company_hints(criteria)`（`app/qualification/criteria.py`）并让公司
分支改用它，两个分支口径终于一致。

修掉的真实影响分三种情况（画像「近 6 个月完成融资的 SaaS 公司，提供订阅制服务」实测）：

| 场景 | 旧（重解析画像） | 新（读冻结标准） |
| --- | --- | --- |
| 规则路径・原画像 | 17 词族 / 无城市 | **完全相同**——规则路径的行业 tokens 本来就是整族，无回归 |
| 规则路径・画像 + 用户补「上海」 | 城市丢失（`preferred_city=None`） | `preferred_city="上海"` |
| LLM 路径・原画像 | 17 词族（字典法） | 7 词（LLM 收敛词，与标准一致） |

**最要紧的是第二行**：用户手动补充的地域条件原本传不到数据源——召回按画像捞、
判定按标准筛，整批系统性落空。城市提示不拼进查询文本（只作 `CompanyQuery.preferred_city`
下发），所以这条修复不会带来查询文本的变化。

`company_hints` **刻意不做同族扩展**（人物侧 `_expand_industry_tokens` 做了）：规则路径的
行业标准 tokens 本来就是整个族，再扩一次是无用功；LLM 路径的 tokens 是模型收敛过的词，
扩回族等于把模型的理解丢掉——而 `_build_search_text` 只取前三个，拼进查询的会是族的前三词。

**已知未修缺陷（2026-09-21 发现，用当前映射离线回放已留档的百度原始返回）**：站点三分类
**识别不出「大厂域名下的内容频道」**。20 条 references 里保留下来的 6 条，有 4 条是内容页：

| 保留的 URL | 实际是什么 | 折成的公司名 |
| --- | --- | --- |
| `cloud.tencent.com`（×2） | 腾讯云开发者社区的文章（「星云有客半年连续完成 3 轮融资」） | `Tencent` |
| `chuangke.aliyun.com` | 阿里云创业频道的新闻（「云砺(票易通)完成近 2 亿美金 C 系列融资」） | `Aliyun` |
| `startup.aliyun.com`（×2） | 阿里云创业平台的融资周报 | `Aliyun` |

根因是 `registrable_domain()` 把 `cloud.tencent.com` 归一成 `tencent.com`，于是它既不在媒体
黑名单、也不在目录站名单里，就被当成「企业官网」，并进一步用域名主体取名。
反例：`market.aliyun.com` 的标题是「武汉特对信息技术有限公司」，是**真的**企业页——
所以不能简单地「只认裸域和 www」，需要一份内容频道子域前缀表（`cloud.` `developer.`
`startup.` `chuangke.` `blog.` `news.` …），且要逐个确认再落表。

**另一个已修缺陷的旁证**：媒体站黑名单修复确实生效。同一份留档返回离线回放，
修复后只映射出 **4 家**（此前 6 家）——`finance.ce.cn`（中国经济网）、`tech.ce.cn`、
`chinastarmarket.cn`（科创板日报）、`thepaper.cn`（澎湃）现在全部被丢弃。
线上列表 `6a5a751d5b89` 里还能看到 `Ce` / `Chinastarmarket` 两条，
是因为该列表创建于 01:18，**早于** 01:29 与 01:46 两次黑名单提交。

**查询文本策略实测（2026-09-21，画像「近 6 个月完成融资的 SaaS 公司，提供订阅制服务」）**：

| 检索文本 | 泛搜索映射后保留 |
| --- | --- |
| 现状：原句 + 字典法前 3 词（`… 企业服务 SaaS saas`） | 5 条 |
| 标准派生：行业3 + 业务2（`SaaS saas 企业服务 融资 完成融资`） | **0 条** |
| 折中：原句 + 行业2 | 2 条 |
| 仅行业3（`SaaS saas 企业服务`） | 3 条 |
| 行业3 + 订阅制 | 6 条 |

**结论**：业务特征词（融资、订阅制）是财经新闻的高频词，塞进查询命中的全是媒体报道——
把 L0 解析出的 `tokens` 直接拼进检索词**不是**提升召回的正解（判定用的词故意取宽，
与检索词要的「少而聚焦」是两种口径）。真正的瓶颈在百度 AI 搜索本身是**网页检索而非企业名录**：
泛搜索段的天花板就这么低，企业名录只能靠爱企查站点限定那一段。

**已修缺陷（2026-09-21）**：媒体站黑名单在 `.com.cn` 域名上**集体失效**——
注册域匹配曾自取最后两段，而 `finance.sina.com.cn` 的最后两段是 `com.cn`，查表永不命中。
漏掉新浪财经、中国经济网、每日经济新闻、环球网等一批站点。修复后上面这些「保留条数」
才是把媒体站真正剔掉之后的诚实数字（此前报告的 40~48% 覆盖率因此被高估）。

**时间窗必须走接口参数，不能拼进查询（2026-09-21 实测的硬证据）**：
留档的 20 条 references 里，网页 `date` 跨 **2020~2026**（2020-04、2021-11、2022-03/04/05/06、
2023-07…），即**百度完全不执行「近 6 个月」这个时间语义**。通道是：

- `search_recency_filter`：`week` / `month` / `semiyear` / `year`；
- `search_filter.range.page_time`：ES 风格相对时间（`gte` / `gt` / `lte` / `lt`）。

⚠️ 它筛的是**网页发布时间**，不等于企业的融资时间——能提高相关性，不能替代判定层的融资条件。
覆盖率杠杆则是 `enable_deep_search=true`（20 → 最多 100 条，代价是 1 次调用变 10 次）。

**同一接口两种模式（只差一个 `model` 字段）**：

| | 不传 `model`（**召回用这个**） | 传 `model`（智能搜索生成） |
| --- | --- | --- |
| 顶层字段 | `request_id`、`references` | 另加 `choices`、`usage`、`is_safe` |
| 内容 | 原始网页结果 | 网页结果 **+** 大模型总结 |
| 成本 | 一次搜索 | 再加几千 tokens（实测 3594~6004），耗时可达 23 秒 |

**召回层必须用纯搜索**：需要可追溯的 URL/标题/摘要/date 才能映射成 `CompanyRecord`、才能
当证据回查。模型总结会**掩盖检索层没有结果这件事**——实测 `ernie-4.5-turbo-32k` 把 2023 年
融资的聚水潭列进「近 6 个月」名单（自己都注明「虽时间略早于窗口…供参考」）。
判定/证据侧则适合智能生成：`deepseek-v4-pro` 能在一次调用里完成「检索 + 时间窗筛选 + 结论」，
且敢报「未能找到严格符合的公司」。但按次计费（23s / 6004 tokens）**不能逐公司调用**。

**契约级缺口（2026-09-21 发现）**：`contract.json` 的 `funding` 维度是
`expected_kind: enum`，取值只有 `未融资 / 天使轮 / A轮 / B轮 / C轮`——**装不下「近 6 个月完成融资」
这类时间窗**。于是 LLM 只能把它塞进 `signal`（`lenient: true`，宽松维度），tokens 也只剩
`融资 / 完成融资 / 新一轮融资 / 获投 / 融资事件 / 战略投资`，**没有任何时间信息**。
结果是用户写「近 6 个月完成融资」，L3 实际只检查「有没有提到融资」，且判不到也仅按宽松记 0.5。
要真正支持时间窗，得给 `funding` 或新维度加一个能表达时间的字段（`expected_kind: range` 之类），
不是调 prompt 能解决的。

**未解问题**：覆盖率上界低，且泛搜索段与爱企查段的贡献比不稳定。
若要提升，方向是**强化爱企查站点限定那段**（提 `top_k`、改用更精确的查询式）而不是
继续调泛搜索的查询文本；仍不宜用合成条目凑数——那是把覆盖率问题从召回层挪到判定层。
