# LLM 在 Ai-get 挖掘链路里做什么、不做什么

一句话：LLM 只负责**把非结构化文本变成结构化判断**，一共三个座位，每个都是**单次调用 + 结构化输出**。
控制流、召回、算分全部归确定性代码。

这条界线的判据来自上游 Revor 的实际形态，也是本项目 P0–P3 的架构前提。

---

## 1. 三个 LLM 座位

### 座位一：L0 生成加权标准

| 项 | 内容 |
| --- | --- |
| 输入 | 用户画像正文（一段自然语言）+ 用户手写的判断条件 |
| 输出契约 | `Criterion[]`：`id` / `name` / `category` / `weight` / `question` / `tokens` / `lenient` |
| 为什么必须是 LLM | 用户的说法千变万化（「杭州的消费品与旅游行业小微商家」），要把它归到**有限的标准维度**上，这是语义归一化，规则表不可能穷举 |
| 当前实现 | **LLM 优先、规则引擎兜底**，两者都已落地：`server/app/llm/`（OpenAI 兼容客户端 + 版本化 prompt + 缓存）、`server/app/qualification/criteria.py` 的 `build_criteria_detailed()`。规则引擎是兜底实现（10 个行业族 + 规模档 + 融资关键词 + 信号词 + 残句兜底）；未开 LLM 或调用失败时自动降级，并把产出方冻结进任务记录 |
| 换 LLM 的成本 | 模块 docstring 明确写了**只需替换这一个函数**——`Criterion` 刻意设计成「一段 JSON 就能表达」的形状，就是为了让规则引擎用的 `tokens` / `lenient` 变成可选字段。实际落地时确实只改了生成侧：`Criterion`、`judge()`、仓储层全部未动 |
| 硬约束 | **必须可复现**。同一份画像每次要产出同一组标准，否则「为什么匹配」不可解释、条件行的色条还会在刷新后跳位 |

产出示例（真实运行结果）：

```
w=5 [geo]          地域：杭州
w=5 [industry]     行业：消费品与零售／文旅与本地服务（任一）
w=3 [size]         规模：小微（50 人以下）
w=3 [funding]      融资阶段：未融资
w=2 [signal]       意向信号：微信小程序
w=1 [reachability] 可触达性
```

### 座位二：L3 逐条判定

| 项 | 内容 |
| --- | --- |
| 输入 | **一条**标准 + 一家企业的证据文本（官网首页／关于我们／联系我们 的正文片段） |
| 输出契约 | `{verdict: 符合｜不确定｜不符合, explanation, reference_count}`，逐条产出 |
| 为什么必须是 LLM | 判断「这家企业的公开资料是否体现了『已上线微信小程序』」本质是**读文本**，规则只能做子串匹配，读不出语义 |
| 当前实现 | `server/app/qualification/judge.py` 的 `_HANDLERS`，按 `category` 分派确定性判定（子串命中、区间覆盖、融资阶段归一化） |
| 换 LLM 的成本 | 替换对应 handler 即可，返回同样的三元组。**`judge()` 的加权与硬性约束逻辑完全不用动** |
| 硬约束 | `lenient` 标准**永远不会判「不符合」**——公开资料没写 ≠ 事实不成立。把「没查到」算成「不符合」，会让所有冷门行业的结果集体掉档 |

### 座位三：展示层文案

| 项 | 内容 |
| --- | --- |
| 覆盖范围 | AI 摘要（列表里那段企业简介）、挖掘策略说明、触达话术与多渠道序列 |
| 为什么必须是 LLM | 纯生成任务，**没有对错，只有好坏** |
| 当前实现 | `server/app/mock/strategy.py`、`server/app/mock/dossiers.py` |
| 关键特征 | **允许失败**。摘要生成失败时后端把 `summary_state` 置为 `failed`，前端渲染出「重试生成摘要」按钮 |

这一个特征把它和座位一、二彻底区分开：
座位一／二失败会让**整批结果不可信**；座位三失败只影响**某一列的内容**，用户可以针对单个字段重试。
所以「字段级重试 UI」只存在于展示层字段上，判定结论没有重试入口。

### 一次挖掘到底是几次 LLM 调用

**座位一 1 次，座位二/三 0 次。实测（`GET /api/llm/status` 的计数增量）：**

| 用户动作 | 接口 | 外部调用 | 缓存命中 |
| --- | --- | --- | --- |
| 建列表 | `POST /targets/lists` | **+1**（约 1400 tokens） | — |
| 挖掘更多 | `POST /targets/lists/{id}/more` | 0 | — |
| 重跑挖掘 | `POST /targets/lists/{id}/mine` | 0 | +1 |
| 保存挖掘配置 | `PATCH /targets/lists/{id}/conditions` | **+1**（约 2300 tokens） | — |
| 再次重跑 | `POST /targets/lists/{id}/mine` | 0 | +1 |
| 上传导入 | `POST /targets/lists/upload` | **+1**（首次） | 第二次 +1 |

> **调用次数与召回多少家企业无关。** 召回 5 家和 25 家的差别只是确定性代码多跑几轮：
> 25 家 × 4 条标准 = 100 次判定，全部由 `judge.py` 用规则算完，**一次外部调用都不产生**。
> 这也是「LLM 出判断、代码出算术」最直接的收益——成本是**每任务一次**，不是**每企业一次**。

假如座位二也交给 LLM（§7.5 的乙-4，尚未实现），同一个 25 家任务会变成
**25 × 4 = 100 次调用**，比现在贵两个数量级。这就是它默认关闭、且只该把规则判「不确定」的那部分
交给 LLM 的原因。

「挖掘更多」是 0 次调用，因为它的 docstring 就写着「沿用同一份标准，只换召回种子」——
新加入的企业拿**同一份标准**判定，结论口径才与首批一致。**标准一旦生成就被冻结，不再重掷。**

---

## 2. LLM 明确不做的四件事

| 不做 | 谁做 | 交给 LLM 会怎样 |
| --- | --- | --- |
| 阶段推进（`generating_criteria → searching → verifying → completed`） | `repositories/targets.py` 的状态机 | 步数不确定、可能死循环；进度四元组（goal/verified/qualified/full）根本无法定义 |
| 决定调哪个数据源 | `providers/registry.py` 按 `priority` 做瀑布式富化 | 同一 query 两次跑出不同的源组合，结果不可复现 |
| 加权算分 | `judge.py` 里的 `total_weight` / `earned` 求和与 `HARD_WEIGHT_THRESHOLD` 判断 | 100 分制打不准；「硬性条件一票否决」这种约束无法强制 |
| 自由 agent 循环 | —— | 成本无界（抓取按条计费）、不可审计、密钥进模型上下文 |

一句话概括这套分工：**LLM 出判断，代码出算术。**

---

## 3. 为什么界线划在这里

三条硬约束，任意一条都足以否定「让 LLM 参与控制流」：

1. **可复现**：同一份画像 + 同一批候选，结论必须一致。这是「为什么这家匹配」可被用户复核的前提。
   讽刺的是，如果判定本身不稳定，那「可解释性」这个卖点就是假的——你解释了，但下次结果不一样。
2. **可审计**：详情页每一条准入条件评估都要能追溯到「哪条标准、判了什么、依据哪一页」。
   所以 `CriterionVerdict` 里带着 `reference_count` 与 `source_url`，`_source_of()` 还会按标准类别挑页面
   （可触达性指向 `/contact`，行业指向 `/about`）。LLM 的自由文本做不到逐条对齐。
3. **成本有界**：抓取按条计费，调用次数必须由我们决定，不能由模型的循环次数决定。

---

## 4. 判定「单次调用」还是「agent」的一句话

> **要 LLM 做「一次判断」→ 单次调用 + 结构化输出（JSON schema 约束）。
> 要 LLM 做「步数不确定的探索」→ 才用 agent。**

挖掘这条链路从头到尾都是「一次判断」，所以没有任何一处需要 agent。
反过来说：如果哪天要做「跟用户对话式地把模糊需求问清楚」，那一步才是 agent 的场合——
因为「还要问几轮」事前不可知。

---

## 5. 代码里可以直接看到的证据

- `criteria.py` 模块 docstring：**「换成 LLM 时只需替换 `build_criteria()` 一个函数」**，
  并说明为什么当前选规则引擎是**为了可复现**（同一份画像产出同一组标准）。
- `judge.py` 模块 docstring：**「判定是确定性的——给定同一份标准与同一条记录，结论永远一致。
  这是『为什么这家匹配』能被前端逐条展示、被用户复核的前提，也是它与『让 LLM 自由发挥』的根本区别。」**
- `providers/contracts.py` 里 `SourceError` 带 `kind`／`retryable`：错误分类学必须在代码里显式建模，
  不能交给模型即兴解释——否则前端无法据此决定「该重试还是该报错」。

---

## 6. 实例：这条画像暴露出的两个能力边界

用一条真实画像把上面的分界测出来：

> **近 6 个月完成融资的 SaaS 公司，提供订阅制服务**

### 规则引擎的真实输出（4 条，总权重 12）

```
[industry    ] w=5 hard=True  lenient=False 行业：企业服务与软件
[business    ] w=3 hard=False lenient=True  业务特征：近 6 个月完成融资的 SaaS 公司
[business    ] w=3 hard=False lenient=True  业务特征：提供订阅制服务
[reachability] w=1 hard=False lenient=False 可触达性
```

两处失效，性质不同：

**失效一：`融资` 被当成虚词丢掉了。** `_INTENT_WORDS` 里有「融资」，本意是防止「融资」这类词
在残句里重复触发标准，但 `FUNDING_KEYWORDS` 只认 `未融资／天使／A轮／B轮／C轮` 这些**阶段词**，
不认「完成融资」这种**事件表述**。两个机制一叠加，整条硬性要求**凭空消失**——
最后连 `criterion-funding` 都没生成，融资维度完全缺位。

**失效二：整条限定语被降级成「业务特征」。** 残留片段变成了
`业务特征：近 6 个月完成融资的 SaaS 公司`——语义已经错了（它把整个句子当成了一个特征名，
还带着「SaaS 公司」的尾巴）。而且 `business` 类标准被标记为 `lenient=True`：

> **`lenient` 的标准永远不会判「不符合」，只会判「不确定」，而「不确定」的得分系数是 0.5。**

### 后果：一家不符合要求的公司被判成「明确符合」

拿一家**普通 SaaS 公司**（有联系人，但对融资时点与收费模式一无所知）跑判定：

```
符合   w=5 factor=1.0  行业：企业服务与软件
不确定  w=3 factor=0.5  业务特征：近 6 个月完成融资的 SaaS 公司
不确定  w=3 factor=0.5  业务特征：提供订阅制服务
符合   w=1 factor=1.0  可触达性

匹配分 = 75 | 结论 = 明确符合
```

得分构成：`5 + 1.5 + 1.5 + 1 = 9`，总权重 12，比值 `9/12 = 0.75`，
**恰好等于（也恰好跨过）`FULL_SCORE_THRESHOLD`**。

反事实：如果这两条能被真正判定并判为不符合，得分是 `5 + 0 + 0 + 1 = 6`，
比值 `0.5`，结论是**可能符合**。

**两条本该判 0 分的标准，因为 `lenient` 保护机制各白送了 1.5 分，把 50 分抬到 75 分，
刚好越过阈值。** 顺带一提，理由文案还会输出「地域与行业等硬性条件均一致」——
但这条画像压根没有地域标准，文案对缺失维度是无感知的。

### 关键结论：这不是「换个更聪明的模型」就能解决的

换成 LLM 之后，标准的**表述**会正确（`融资事件 · 近 6 个月` 会被识别成一个带时间窗的独立维度），
但**判定仍然做不出来**，因为缺两样东西：

| 缺什么 | 在哪一层 | 具体缺什么 |
| --- | --- | --- |
| **schema 缺字段** | `qualification/criteria.py` | `Criterion` 没有时间窗字段（如 `window_days=180`），LLM 就算输出对了也无处安放 |
| **数据源缺时间戳** | `providers/contracts.py` | `CompanyRecord` 只有 `funding_stage`（一个阶段标签），**没有融资发生时间**。`WebDocument` 倒是带 `published_at`，但归一化后的记录没有把它提取成结构化字段 |

也就是说：**LLM 负责把标准写对，但让标准「判得出来」是 schema 与数据源的活儿。**
对应到路线图，这属于 P5（接企业库补齐字段）之外还需要单独补的一次**契约扩展**——
建议在 `CompanyRecord.attributes` 里先落 `last_funding_at`（这个字段已经是既有的扩展位），
并在 `Criterion` 上加 `window` 字段，判定层再按时间窗比较。

### LLM 会输出什么

对同一句画像，一个受 JSON schema 约束的 LLM 调用应当产出（示意）：

```json
[
  {"category": "industry",       "weight": 5, "tokens": ["SaaS", "企业服务", "软件"],
   "question": "企业是否为 SaaS 或企业软件服务商？"},
  {"category": "funding_event",  "weight": 4, "window": {"days": 180},
   "question": "企业在最近 180 天内是否完成过一轮融资？",
   "note": "需要带时间戳的融资事件数据，funding_stage 不足以判定"},
  {"category": "business_model", "weight": 3, "tokens": ["订阅", "SaaS", "按年付费", "续费"],
   "question": "企业的主要收入是否为订阅制（经常性收入）？"},
  {"category": "reachability",   "weight": 1,
   "question": "是否能获取到可用的公开联系方式？"}
]
```

与规则引擎输出的差别不是「更聪明」，而是**四件具体的事**：

1. 新增了一个维度 `funding_event`，且**显式带时间窗**——规则引擎里连这个维度的位置都没有。
2. 把「订阅制」归到 `business_model`，与 `industry` 分开——规则引擎把它塞进了笼统的 `business`。
3. `weight` 是**判断出来的**（融资时点是强约束 → 4），而规则引擎的权重是一张**固定表**按 category 查的。
4. 主动标注了 `note`：这条标准**当前数据源判不出来**。这是最容易被忽略但最有价值的一点——
   LLM 可以把「不可判定」当成一个结论输出，而不是默默降级成 lenient。

---

## 7. 两条路线的落地规划

「不上 LLM」和「补上 LLM」不是二选一的两条平行路——**它们的第一步是同一件事**，
而且其中一步同时是两条路线的公共依赖。

### 7.0 共同前置：让分数诚实（这一步必须先做）

现在的 `lenient` 语义是：「未验证 = 0.5 分」。它保护了冷门行业不被误伤，代价是**制造假阳性**——
一家完全不符合要求的公司能拿到 75 分「明确符合」（见第六节实测）。

**⚠️ 一个必须先验证的陷阱：不要试图「把不可判定的标准从分母里剔除」。** 实测结果：

| 方案 | 该样例得分 | 结论 | 评价 |
| --- | --- | --- | --- |
| 现状：未验证 = 0.5 分 | 75 | 明确符合 | 假阳性 |
| 可判定项归一化（剔除分母） | **100** | 明确符合 | **反而更糟**：剩下的全是命中的，比值变 1.0 |
| **未验证 = 0 分 + 禁止「明确符合」** | **50** | 可能符合 | ✅ 正确方向 |

正确做法是两条规则同时上：

1. **未验证的 `lenient` 标准记 0 分**（而不是 0.5 分）。
2. **只要存在未验证的 `lenient` 标准，结论上限锁在「可能符合」**——复用 `is_hard` 一票否决的同一机制。
   理由：用户写出来的是**要求**，不是**偏好**；要求没被验证，就不该说「明确符合」。

对整批结果的影响（25 家候选，同一条画像）：

| 方案 | 明确符合 | 可能符合 | 待确认 |
| --- | --- | --- | --- |
| 现状 | 6 | 0 | 19 |
| 修正后 | 0 | 6 | 19 |

**修完会暴露两个比评分更根本的问题**，这才是这一步的真正价值：

- **19/25 是「待确认」→ 召回质量问题。** 实测这 25 家的行业分布分散在 20 个行业上
  （食品 3、餐饮 3、人工智能 2……），行业标准命中率只有 **6/25 = 24%**。
  即 `industry_hints()` 算出来了，但「无城市画像」时召回没有真正遵守它。
  **这是当前对结果影响最大的一条，优先级高于评分公式**，而且不花一分钱。
- **6 家上不去「明确符合」→ 数据源缺字段问题。** 它们卡在两条无法验证的门槛项上，
  需要 §6 说的 `funding_event` 时间窗与 `business_model` 字段才能判出来。

> **2026-09-20 更新：第一条已修，见 §7.6。** 根因既然已经用离线模拟量化到具体数字（24%），
> 「先修评分、让召回问题自己暴露出来」这个理由就不再成立；而本节自己也写了召回
> 「优先级高于评分公式，且不花一分钱」。于是落地顺序调整为
> **召回相关性（已完成）→ 7.0 分数诚实 → 甲-1 受控词表**，见 §7.4。

一句话：**修评分不是为了提分，是为了让缺口以正确的方式暴露出来**，而不是被一个 75 分的假信号盖住。

### 7.1 路线甲：不上 LLM

核心思路：**语义归一化是 LLM 的活儿，那就不做语义归一化——把语义交还给用户。**

| 步骤 | 做什么 | 为什么 |
| --- | --- | --- |
| 甲-1 | 建 `criteria_catalog` 受控词表 + 匹配器注册表，`build_criteria` 改为「按目录求值」 | 真扩展性：新增行业/信号只往词表加词，不改代码。匹配器只需 5 种：`enum_in` / `substring_any` / `range_cover` / `city_in` / `date_within` |
| 甲-2 | 前端改**结构化筛选器**：地域选择器、行业树多选、规模区间、融资阶段多选、时间窗、业务模型标签 | 把「自然语言 → 结构」的活儿交还给用户点击。**用户的校对成本远低于解析错误的成本** |
| 甲-3 | 画像文本框降级为**可选补充**，解析结果以 chip 形式让用户确认/删除 | 解析错误变成用户可修正的，而不是隐藏的错误 |
| 甲-4 | 判定改为在 `evidence[].text` **全文检索**并回传命中片段，替代现在的 `token in summary+name+industries` | 即使没有 LLM，「为什么匹配」也要有真实依据 |
| 甲-5 | 展示层文案模板化（策略说明、触达话术用模板 + 变量拼接） | 承认这块做不到自然，别假装 |

**拿不到什么**：面对非受控表达只能降级为「请选择」，产品从「AI 挖掘」变成「高级筛选器」；
文案做不到参考站那种自然；差异化卖点（语义解释）会变弱。

**换来什么**：完全确定性、零边际成本、零密钥托管、可离线、结论 100% 可复现。
对「结果要进 CRM、要给销售直接用」的场景，这几条可能比语言能力更重要。

**适用判断**：Ai-get 当前阶段（复刻 + 观感）不上 LLM 完全够用——
P1–P3 的观感提升主要来自**标准被展示出来**，而不是标准生成得多聪明。

### 7.2 路线乙：补上 LLM

核心思路：**LLM 出判断，代码出算术**；并且**把不确定性关在「任务创建」那一刻**。

| 步骤 | 做什么 | 关键设计 |
| --- | --- | --- |
| 乙-1 | `StructuredGenerator` 抽象 + `LlmGenerator` + **三元降级链** + 输出白名单校验 | 降级链 `LlmGenerator → RuleGenerator → 保守默认`。LLM 超时／报错／输出不合 schema 时自动降级，**绝不阻塞任务**。这正好复用 `providers/` 的 `SourceError` 分类学——把 LLM 当成一个会失败的外部依赖 |
| 乙-2 | schema 扩字段 + 数据源补时间戳 | `Criterion` 加 `window`（时间窗）、`dimension`（细化维度如 `funding_event`/`business_model`）、`judgeable`（能否用现有数据判定）；`CompanyRecord.attributes` 落 `last_funding_at` |
| 乙-3 | `build_criteria` 换 LLM 实现 + **结果冻结落库** + prompt 版本化 + 缓存 | 见下方「可复现性」 |
| 乙-4 | L3 判定改「**规则优先 + LLM 兜底**」 | 只把规则判「不确定」的那些标准交给 LLM。25 家 × 6 条 = 150 次调用太贵；规则能判的继续用规则，省钱且可复现 |
| 乙-5 | 展示层文案交 LLM，失败降级到模板 | 复用 §1 座位三的「允许失败 + 字段级重试」语义 |

**可复现性是路线乙的核心难点**——LLM 不确定，但产品承诺确定。解法是四件事：

1. `temperature=0` + 结构化输出（JSON schema / function calling），不用自由文本。
2. **结果落库并冻结**：标准一旦生成就写进任务记录，后续所有判定都用这份**冻结的**标准。
   **绝不在每次判定时重新生成标准。**
3. **prompt 必须版本化**（版本号写在提示词技能目录的 `contract.json` 里，当前 `criteria/v1`），
   并把 `prompt_version` + `model` 一起存进任务。
   前端可以展示「本次标准由 criteria/v1 生成」。否则改 prompt 后历史标准对不上。
4. 提供显式的「**重新生成标准**」按钮（就是现在的 `remine`），把不确定性变成用户可控的动作。

> **不确定性被隔离在任务创建的那一刻，且用户可主动重掷；之后的链路完全确定。**

**白名单校验绝不能省**：LLM 输出的 `category` 必须 ∈ 枚举、`weight` ∈ 1..5、时间窗 ≤ 上限，
**不合规就降级到规则引擎**，不信任自由输出。

### 7.3 两条路线的取舍对照

| 维度 | 路线甲：不上 LLM | 路线乙：补上 LLM |
| --- | --- | --- |
| 结论可复现 | 天然 100% | 需靠冻结 + temperature=0 + 版本化 |
| 边际成本 | 0 | 每次生成 1 次调用 |
| 密钥托管 | 无 | 需要 |
| 非受控表达 | 只能降级为「请选择」 | 能直接吃下 |
| 展示层文案 | 模板拼接 | 自然 |
| 工程复杂度 | 低 | 中（降级链 + 校验 + 缓存 + 版本化） |
| 适用场景 | 结果要进 CRM／要可复核／要离线 | 面向用户首次探索、需要「像人一样懂我」 |

### 7.4 建议的落地顺序（2026-09-20 调整）

**先做召回相关性（§7.6，已完成）→ 再做 7.0 分数诚实 → 最后是甲-1 受控词表。**

原计划让 7.0 排第一的理由是「修完评分，召回问题才会暴露」。但该问题已经用离线模拟量化到
具体数字（无城市画像的行业命中率 24%），**「靠修评分来暴露它」的前提消失**；而 §7.0 自己也写了
召回「优先级高于评分公式，且不花一分钱」。所以把它提到最前。

后两步的顺序不变，理由是第二个收口：

> **甲-1 的受控词表（`criteria_catalog`），正好是乙-1 输出白名单校验的数据来源。**

做一次，两条路线都受益：规则引擎靠它获得可扩展性，LLM 靠它做输出校验。
所以不存在「先走甲、以后改乙会白做」的问题——**甲-1 是两条路线的公共资产**。

### 7.5 实施进度（截至当前代码）

路线乙的**座位一已经落地**，其余仍是规划。逐条对照：

| 步骤 | 状态 | 实际做法与偏差 |
| --- | --- | --- |
| 乙-1 降级链 + 白名单校验 | ✅ 已落地 | `app/llm/`（客户端 + 版本化 prompt + 缓存 + 统计）、`criteria.build_criteria_detailed()`。**降级链只有两级**（LLM → 规则引擎），没做第三级「保守默认」——规则引擎在任何输入下都能返回一份可用标准，再加一级没有增量价值 |
| 乙-2 schema 扩字段（`window` / `dimension` / `judgeable`） | ❌ 未做 | 时间窗类条件（「近 6 个月完成融资」）目前仍是 `business` 维度下的自由文本，`judge.py` 只能做子串匹配，判不了时间范围 |
| 乙-3 结果冻结 + prompt 版本化 + 缓存 | ✅ 已落地 | 冻结方式改为写进**任务记录**（`criteria_source` / `criteria_label` / `criteria_fallback_reason`）——本项目没有数据库，与「内存单例」的整体约定一致。`PROMPT_VERSION = "criteria/v1"`（不是规划里的 v3）。缓存键为 `(prompt_version, model, 画像, 用户条件)`，实测同查询第二次命中缓存、零外部调用 || 乙-4 L3 判定「规则优先 + LLM 兜底」 | ❌ 未做 | 只有配置开关 `AIGET_LLM_JUDGE_ENABLED`（默认 `false`），判定仍是纯规则 |
| 乙-5 展示层文案交 LLM | ❌ 未做 | AI 摘要等仍是本地模板 |
| 7.0 让分数诚实（未校验的 lenient 记 0 并拦住「明确符合」） | ❌ 未做 | **这是路线乙的前置条件，目前仍是缺口**：`lenient` 未命中给 0.5 而非 0，一家不符合要求的公司仍可能被抬进「明确符合」。标准生成换成了 LLM，但打分口径没变 |

**提示词已外置到技能目录。** L0 的 system / user 提示词与机器可读契约不再内嵌在 `app/llm/prompts.py`，
而是放在 `skills/profile-to-weighted-criteria/`：

| 文件 | 角色 |
| --- | --- |
| `contract.json` | ★ 唯一真源：维度枚举、每维度上限、`expected_kind`、`enum_values`、`lenient`、条数区间、`version` |
| `prompts/criteria.system.md` / `criteria.user.md` | 提示词骨架，只含 `$占位符`；契约表由渲染器生成 |
| `scripts/validate_criteria.py` | 独立白名单校验器，读同一个 `contract.json` |
| `references/prompt.md` | 渲染结果快照（逐字）+ 移植指南 |
| `references/pitfalls.md` | 实测踩坑清单 |

这样做的直接收益：**「模型按什么契约输出」与「校验器按什么契约拒绝」永远同源**——
过去契约在提示词里手抄一份、在校验器里再抄一份，改一处忘另一处就表现为
「模型输出全被拒 → 整批降级」，看着像模型故障，实际是配置漂移。

契约与规则引擎自己的表（`_LLM_CATEGORIES` / `_MAX_PER_CATEGORY` / `FUNDING_KEYWORDS` 的各阶段）
在加载时**逐项断言**，不一致直接抛 `PromptAssetError`。理由：提示词属于**仓库资产**而非会抖动的
外部依赖，缺失或自相矛盾说明检出损坏，应当立刻炸掉——若容忍它降级，所有人都会以为 LLM 正在工作，
而实际上提示词根本没被读到。默认目录 `skills/profile-to-weighted-criteria`（相对仓库根解析），
可用 `AIGET_LLM_PROMPT_DIR` 指向另一份技能目录以换领域，无需改代码。

实测（`/api/llm/status` + 一次真实建任务）：`prompt_version=criteria/v1`、
`prompt_dir=skills/profile-to-weighted-criteria`、`criteria_source=llm`、
`criteria_label=LLM · criteria/v1 · deepseek-flash`、`calls=1 / failures=0`；
把该次模型输出喂给技能自带校验器 → `✓ 4 条标准全部合规（契约 criteria/v1）`。

> **但「可复现」只兑现了一部分**：同一句画像连跑三次，`temperature=0` 下模型给出的
> 标准名并不相同——「行业：SaaS 软件服务」/「行业：SaaS与企业服务软件」/「行业：SaaS 软件公司」。
> 冻结设计保证了**一个任务内部**自始至终用同一份标准（这才是产品承诺的那部分），
> 但**两个用同一句画像建的任务，标准不会一模一样**；缓存只在同一进程内掩盖这个差异。
> 由此带来一个可见后果：标准的 `id` 是名字的 CRC32，名字一变 id 就变，
> 于是两个任务的条件色条分配可能不同。跨任务的「同样输入同样结果」并未成立。

> 换句话说：**这次接入解决的是「标准从哪来」，没有解决「分数准不准」。**
> 前者是可见的能力升级，后者是 §6 里那条真实错判的根因，两者不能互相替代。

### 7.6 召回相关性：已落地（2026-09-20）

**根因**：`providers/mock_source.py` 的 `select_seeds()` 在 `preferred_city` 为空时**直接整池轮转**，
`industry_tokens` 根本没参与排序——**参数收了不用**。而有城市的分支会 `matched` 优先，
两个分支的排序规则不对称。于是「消费品行业的商家」这类不带城市的画像会召回到一堆
人工智能与工业软件公司，行业标准成批落空；看起来像判定层不准，实际是召回层的问题。

**改法**：把两个分支合成一条路径——**城市只负责缩小池子，行业负责排序**；没有城市时池子就是全部语料。
`industry_tokens` 为空时退化为「整池按 seed 轮转」，与改动前逐字一致
（用 32 个用例的基线对比验证，其中 16 个无行业词用例**零差异**）。

**同时扩了语料**，因为排序修好后，命中率的**上限就是语料对该行业的覆盖**：
`mock/company_corpus.py` 从 **39 家 → 85 家**，补齐文旅/零售/餐饮/农产品/本地服务/轻工制造等线下行业，
并把 10 个行业族里最薄的两族（新能源与环保、教育与培训）从 1~2 家补到 7 家；
`synth.INDUSTRY_PACKS` 从 10 个包加到 17 个，让 100/500/1000 条的大批量合成也能落在传统行业里。

**实测（`count=25`、无城市、seed 0~3 取平均）：**

| 画像 | 改前 | 改后 |
| --- | --- | --- |
| 消费品、旅游等非互联网传统行业小微商家 | 24% | **100%** |
| 深圳的跨境电商物流服务商 | 24% | **100%** |
| 杭州的 SaaS 企业服务公司 | 24% | 44% |
| 上海生物医药 CRO 企业 | 24% | 28% |
| 成都的环保科技企业 | 24% | 28% |

改前**无论画像是什么都是 24%**——因为排序里根本没有行业信号，命中率只反映语料的行业平均分布。
改后命中率**随画像变化**：画像与语料覆盖匹配得好就高。

剩下那些 28%~44% 的数字**不是新缺陷，而是语料覆盖的诚实反映**。这与 §九.6 的
「每个数据源都有覆盖盲区，应该照做标注 coverage limitation」一致：
**不要为了数字好看用合成条目去凑高命中率**，那等于在 UI 上假装全网覆盖。

**端到端实测**（真实 LLM 建任务，画像即上表第一行）：`criteria_source=llm`、
`LLM · criteria/v1 · deepseek-flash`、`fallback_reason` 为空；25 条召回里 **24 条行业相关（96%）**，
唯一不相关的「深圳前海智仓供应链」（仓储物流）被判定层打 32 分、标为**待确认**——
召回给出的噪声被判定层正确拦下，这正是「召回宽、判定严」应有的形状。

> 顺带一个观察：这次 25 条里有 **3 条「明确符合」**（顶部的 92/84/82 分）。
> 它们是在 7.0 尚未落地的情况下产生的，说明**未验证的 lenient 标准仍在把结论往上抬**。
> 这是 7.0 要解决的问题，不是本次改动引入的。

---

## 8. 接入后的实测：LLM 让错判变严重了

用同一句画像（§6 那句）跑两套标准、同一批召回记录，对照判定结果。**这是接入后真实跑出来的数据**：

| 公司 | 规则引擎 | LLM 标准 | 融资阶段 |
| --- | --- | --- | --- |
| 杭州云栖智能科技 | 75 明确符合 | **88 明确符合** | B 轮 |
| 南京律格企业咨询 | 75 明确符合 | **88 明确符合** | 未融资 |
| 无锡江南智造工业软件 | 75 明确符合 | **88 明确符合** | B 轮 |
| 杭州云启企业服务 | 75 明确符合 | **88 明确符合** | 未融资 |
| 上海智联企业服务 | 75 明确符合 | **100 明确符合** | 天使轮 |
| 杭州西湖智联网络技术 | 71 可能符合 | **83 明确符合** | 天使轮 |

一句话：**分数整体抬高 13 分，「未融资」的公司照样是「明确符合」。**

### 根因不是 LLM 不够聪明，是打分口径没改

以南京律格（一家法律咨询机构，未融资）为例，逐条对比它凭什么拿分：

| 标准 | 规则引擎判定 | LLM 标准判定 |
| --- | --- | --- |
| 行业（w5） | 符合 1.0 | 符合 1.0 |
| 订阅制（w3） | **不确定 0.5** | **符合 1.0** ← 分数就是这里涨的 |
| 融资（w3） | 不确定 0.5 | 不确定 0.5 |
| 可触达性（w1） | 符合 1.0 | 符合 1.0 |
| **合计** | **75** | **88** |

差别来自 tokens 表：

```
规则引擎  订阅制 -> ['提供订阅制服务']                    # 整句，命中不了
LLM      订阅制 -> ['订阅','订阅制','付费订阅','按年付费','年费',...,'SaaS']  # 11 个同义词
```

而这家公司的摘要写的是「**通过线上订阅加线下顾问的模式**服务长三角成长型企业」——
一个咨询顾问模式的合同措辞，被 `订阅` 这个通用词命中，直接判成「提供订阅制服务」。

**所以 LLM 的宽同义词表是一把双刃剑**：

- 好的一面：行业维度召回率显著提升（这正是 §6 里 24% 命中率问题的解药）；
- 坏的一面：**它把「弱命中」升级成了「明确符合」**。规则引擎用整句做 token 时反而歪打正着地避开了这类误判。

### 由此得到的第二条必修项

§7.0 说的「未校验的 lenient 记 0」只解决了一半。实测暴露出另一半：

> **单 token 子串命中 ≠ 语义确认。**「命中一个通用词就给满分 1.0」这个口径，
> 在词表变宽之后会批量制造假阳性。

判定因子需要区分强度，而不只是三档：

| 强度 | 场景 | 建议 factor |
| --- | --- | --- |
| 强命中 | 多个 token 同时命中，或命中专指性强的 token（`按年付费`、`ARR`） | 1.0 |
| 弱命中 | 只命中一个通用词（`订阅`、`软件`、`服务`） | 0.5（不确定） |
| 无命中 | 文本里没有对应表述 | 0（当前是 0.5，见 §7.0） |

这条改动落在 `judge.py`，与 LLM 无关——**换成更聪明的模型也不会自动修好它**。
顺序仍然是 §7.4 说的：先做 7.0（打分口径），再谈 7.2（生成侧）。

紧接着做甲-4（证据全文检索），因为它同样与有没有 LLM 无关，且直接提升可解释性。

真正需要决策分叉的时刻是**要不要接受「非受控表达只能请用户下拉选择」这个产品形态**——
如果目标是复刻参考站那种「一段话就能挖」的体验，就必须走乙；如果能接受表单化，甲足够。


