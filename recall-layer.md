# 召回层（L1）是什么

> 这是概念说明，不是实现文档。实现细节见 `p6-implementation-plan.md`，
> 分层架构与上游对照见 `company-discovery-plan.md` 第五节。

## 一句话

**召回层是「从海量数据里把可能相关的候选捞出来」的那一层。**
它只保证**不漏**，不保证**都对**——对错由 L3 判定层负责。

在 `company-discovery-plan.md` 的 L0–L4 架构里就是 **L1 候选召回**：

| 层 | 做什么 | 本项目产出量（示例画像） |
| --- | --- | --- |
| L0 资格标准引擎 | 画像 → 加权 `criteria[]` | 1 次 LLM 调用 |
| **L1 候选召回** | **向数据源要一批可能相关的候选** | **20 条网页 → 4~6 家候选** |
| L2 字段补齐 | 瀑布式补规模、行业、融资、联系方式 | 缺什么补什么 |
| L3 资格验证 | 逐条 `criteria` 判 full / partial + 打分 | 明确符合 / 可能符合 / 不符合 |
| L4 可选增强 | 海关贸易、新闻、招投标 | 按需接入 |

## 一、为什么必须拆成两层

搜索引擎和推荐系统都是这个形状：先用倒排索引或向量检索从亿级文档里捞出几千条
（**召回**，宽而快），再逐条精算相关度排出前 10（**排序**，窄而准）。

原因是**宽和准天生冲突**：能把关严的算法跑不到亿级数据上，能跑亿级的一定粗糙。
所以只能分两层，各自用最合适的口径。

| | 召回层 | 判定层 |
| --- | --- | --- |
| 口径 | 宽：宁滥勿缺，可错杀不可漏 | 严：硬性条件一票否决 |
| 手段 | 关键词检索、域名与站点归类 | 逐条 `criteria` 比对 + 证据来源 |
| 错了的代价 | 漏掉的公司**再也不会出现** | 多留几家只是多浪费一次触达 |

最后一行是这套分工的**根本理由**：两边的错误代价不对称。所以「召回宽、判定严」
不是风格偏好，而是由代价结构推出来的结论。

## 二、召回层的三条判据

一个设计得对的召回层，看这三个特征就够：

1. **宽**——宁可带回 20 条里有 15 条是噪声，也不要为了干净而漏掉真目标；
2. **便宜**——按次计费的数据源，一次挖掘的调用次数必须是常数（本项目：1 次 LLM + N 次检索），
   不能随候选数量增长；
3. **可追溯**——每条候选必须带 URL、标题、摘要、日期。后面要映射成 `CompanyRecord`、
   要当判定层的证据回查。

## 三、在这份仓库里，召回层具体是什么

调用链（公司侧）：

```
_repositories/targets.py::_recall()
        │  CompanyQuery(text, preferred_city, industry_hints, names, limit…)
        ▼
providers/__init__.py::company_source()     取当前公司召回源（AIGET_COMPANY_SOURCE）
        ▼
providers/registry.py::call_source()        计时 / 计价 / 异常收敛成 SourceError
        ▼
providers/baidu_source.py::search_companies()
        │  两阶段检索（泛搜索 → 爱企查站点限定补量）+ 进程内缓存
        ▼
providers/web_mapping.py                    站点三分类 / 取名闸门 / 注册域去重
        ▼
list[CompanyRecord]  ──▶ providers/enrichment.py::enrich_companies()（L2）
```

要点：

- **提示词从冻结的标准派生**，不重新解析画像：`company_hints(criteria)`（会社）、
  `person_hints(criteria)`（人物）。标准才是这条任务的真源——用户可能刚改过条件。
  ⚠️ 城市这类提示只走 `CompanyQuery.preferred_city`，**不拼进查询文本**。
- **产出物是 `CompanyRecord`**（`providers/contracts.py`）：名称、域名、行业、
  以及 `attributes` 里的照面字段。判定层唯一的抓手是**名称**，所以取不出名称的候选直接丢弃。
- **不补数量**。数据源给多少就是多少；「凑数」是召回层自己的事，
  不该由状态机悄悄替它完成，更不许用合成条目充数（那会把覆盖率问题从召回层挪到判定层）。

## 四、它**不**做什么

| 不是召回层的事 | 属于 |
| --- | --- |
| 判断这家公司符不符合资格 | L3 判定层 |
| 补规模、融资阶段、联系方式 | L2 补齐层 |
| 排序、打分、决定展示顺序 | L3 + 前端 |
| 决定要多少家（`requested_count`） | 状态机 / 用户输入 |

## 五、怎么一眼看出「这是召回层的问题」

| 现象 | 归属 | 不要去做的事 |
| --- | --- | --- |
| 结果里全是资讯站、榜单页 | 召回层 | 去调判定 prompt / 评分公式 |
| 目标公司压根没出现在任何一条 | 召回层（或语料上界） | 放宽判定阈值 |
| 召回数量远低于目标（如 6/25） | 召回层覆盖上界 | 用合成条目凑数 |
| 带回来的公司行业整体跑偏 | 召回层提示词 | 让判定层去扣分 |
| 同一家出现两次 | 召回层去重键 | 前端去重 |

判据很简单：**问题出在「有没有被带回来」就是召回层；出在「带回来之后判得对不对」才是判定层。**

### 本项目的三个真实例子

1. **覆盖率上界**。画像「近 6 个月完成融资的 SaaS 公司，提供订阅制服务」目标 25 家，
   实际只召回 6 家。离线回放那 20 条 references 后发现：里面**几乎没有真实目标企业**
   （只有有赞、personality.cool 算官网）——根因是百度 AI 搜索是**网页检索而非企业名录**，
   天花板不在查询写法上。
2. **站点归类漏网**。媒体站黑名单曾在 `.com.cn` 域名上集体失效
   （`finance.sina.com.cn` 取最后两段是 `com.cn`，查表永不命中）；
   修好后仍有「大厂域名下的内容频道」被当成企业官网
   （`cloud.tencent.com`、`chuangke.aliyun.com`、`startup.aliyun.com`）。
3. **提示词丢失**。公司侧曾用 `detect_city(query)` 重解析画像，
   导致用户手动补充的地域条件传不到数据源 → 召回按画像捞、判定按标准筛，整批系统性落空。

## 六、三个常见误解

**① 把召回问题当判定问题。**
「找消费品企业却返回一堆工业软件公司」看起来像判定不准，实际是召回没带对候选。
判定层的职责是**区分**候选到不到位，不是**替召回层兜底**。

**② 以为把解析出来的标准拼进查询就能提升召回。**
实测反例（同一画像，泛搜索映射后保留条数）：

| 检索文本 | 保留 |
| --- | --- |
| 原句 + 字典法前 3 词（`… 企业服务 SaaS saas`） | 5 条 |
| 标准派生：行业 3 + 业务 2（`… 融资 完成融资`） | **0 条** |
| 折中：原句 + 行业 2 | 2 条 |
| 仅行业 3 | 3 条 |
| 行业 3 + 订阅制 | 6 条 |

「融资」这类词是财经新闻高频词，塞进查询命中的全是媒体报道。
判定用的 `tokens` 故意取宽（判严格的东西不该靠词表），
与检索要的「少而聚焦」是**两种口径**——不能互相挪用。

**③ 以为召回层可以让 LLM 兜底。**
LLM 的总结会**掩盖「检索层没有结果」这件事**：实测 `ernie-4.5-turbo-32k`
把一个 2023 年融资的公司列进「近 6 个月」名单（自己都注明「虽时间略早于窗口…供参考」）。
召回层要的是可追溯的原始结果，不是一段读起来合理的总结。

## 七、相关文档

- `llm-role.md`——LLM 在这条链路里坐哪三个位子、不做什么；
- `company-discovery-plan.md` 第五节——L0–L4 分层与上游（Websets / Revor）对照；
- `p6-implementation-plan.md`——数据源选型、覆盖率实测与已修缺陷。
